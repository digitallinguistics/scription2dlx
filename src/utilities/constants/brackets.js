/**
 * A hash of which brackets are used for each type of line
 */
export default {
  grouping:    [`[`, `]`],
  infix:       [`<`, `>`],
  lit:         [`[`, `]`],
  phon:        [`[`, `]`],
  tlnCurly:    [`‘`, `’`],
  tlnStraight: [`'`, `'`],
  txn:         [`/`, `/`],
}
