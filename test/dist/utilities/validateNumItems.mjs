import"core-js/modules/es.array.iterator";import"core-js/modules/web.dom-collections.iterator";export default function validateNumItems(lines){const itemLists=Object.values(lines);const numItems=Math.max(...itemLists.map(w=>w.length));const sameNumItems=itemLists.every(list=>list.length===numItems);if(!sameNumItems){throw new Error("All morpheme and glosses lines must have the same number of words and morphemes.")}}